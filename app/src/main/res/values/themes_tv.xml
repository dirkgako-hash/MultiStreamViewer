<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme -->
    <style name="Theme.MultiStreamViewer" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">#FF1565C0</item>
        <item name="colorPrimaryDark">#FF0D47A1</item>
        <item name="colorAccent">#FF64B5F6</item>
        <item name="android:windowBackground">@color/background_black</item>
    </style>

    <!-- No ActionBar + FullScreen (used in Manifest) -->
    <style name="Theme.MultiStreamViewer.NoActionBar.FullScreen" parent="Theme.MultiStreamViewer">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@color/background_black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
